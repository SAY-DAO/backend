get need participants
Returns information of all need participants as a json object.
Calling this API affects need, need_family table and user tables.
---

tags:
  - Need API

parameters:
  - in: path
    name: need_id
    required: true
    type: integer

responses:
  500:
    description: Error occurred somewhere!

  200:
    description: Ok

    schema:
      id: user_model