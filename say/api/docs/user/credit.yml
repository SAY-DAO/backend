get user credit
Returns user's credit amount.
Calling this API affects user table only.
---

tags:
  - User API

parameters:
  - in: path
    name: user_id
    required: true
    type: integer

responses:
  500:
    description: Error occurred somewhere!

  200:
    description: Ok

    schema:
      properties:
        credit:
          type: integer
