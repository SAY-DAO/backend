get all users needs
Returns all the needs that all users' children have.
Calling this API affects family, user_family, user, child_need, need and need_family tables.
---

tags:
  - User API

responses:
  500:
    description: Error occurred somewhere!

  200:
    description: Ok

    schema:
      id: need_model
