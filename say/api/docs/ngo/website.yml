get ngo by website
Returns information of an NGO with given website as a json object.
Calling this API only affects ngo table by selecting a single row from it.
---

tags:
  - NGO API

parameters:
  - in: path
    name: website
    required: true
    type: string

responses:
  500:
    description: Error occurred somewhere!

  200:
    description: Ok

    schema:
      id: ngo_model
