get ngo by name
Returns information of an NGO with given name as a json object.
Calling this API only affects ngo table by selecting a single row from it.
---

tags:
  - NGO API

parameters:
  - in: path
    name: name
    required: true
    type: string

responses:
  500:
    description: Error occurred somewhere!

  200:
    description: Ok

    schema:
      id: ngo_model
