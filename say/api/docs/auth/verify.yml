Verify API
---

tags:
  - Auth API

parameters:
  - in: path
    name: user_id
    required: true
    type: integer

  - in: formData
    name: verifyCode
    required: true
    type: string

responses:
  500:
    description: Error occurred somewhere!

  200:
    description: Ok
    schema:
      properties:
        accessToken: 
          type: string
        refreshToken:
          type: string
        user: 
          schema:
            id: user_model
