Verify Phone API
---

tags:
  - Auth API

parameters:
  - in: formData
    name: phone_number
    required: true
    type: string

responses:
  400:
    description: Missing Phone Number, Invalid Phone Number, Phone Number Exists

  200:
    description: Ok, A code will send to the phone number
