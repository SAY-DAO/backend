List of Change Cost for a specific need
---

tags:
  - Change Cost API

parameters:

  - name: Authorization
    in: header
    description: Access Token
    required: true
    type: string

  - in: path
    name: need_id
    required: true
    type: integer


responses:

  200:
    description: OK

    schema:
      id: change_cost

  404:
    description: Need not found

