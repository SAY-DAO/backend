get child say name
Returns child's SayName.
Calling this API affect s on child table only.
---

tags:
  - Child API

parameters:
  - in: path
    name: child_id
    required: true
    type: integer

responses:
  500:
    description: Error occurred somewhere!

  200:
    description: Ok

    schema:
      properties:
        sayName:
          type: integer
