get child family
Returns information of all child's family members as a json object. Json key is user id and json value is user data.
Calling this API affects on family, user_family and user tables.
It contains user info and their role.
---

tags:
  - Child API

parameters:
  - in: path
    name: child_id
    required: true
    type: integer

responses:
  500:
    description: Error occurred somewhere!

  200:
    description: Ok

    schema:
      # TODO: make it better
      id: user_model
