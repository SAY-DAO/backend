Get a receipt

---

tags:
  - Receipt API

parameters:
  - name: Authorization
    in: header
    description: Access Token
    type: string

  - name: id
    in: path
    required: true

responses:
  200:
    description: receipt
